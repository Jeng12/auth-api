# Production env vars are injected by the host (Vercel dashboard).
# This file must exist so Laravel's phpdotenv can open it.
# Do NOT add secrets here.

# Safe serverless defaults (overridden by Vercel dashboard env vars if set).
LOG_CHANNEL=stderr
CACHE_STORE=array
SESSION_DRIVER=array
