# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository (`auth-api`) is in its initial state. As of the last update, only a `README.md` exists. No language, framework, tooling, or conventions have been established yet.

When code is added, update this file to document:
- Build, lint, and test commands (including how to run a single test)
- The chosen language/runtime and package manager
- High-level architecture decisions (e.g., framework, database, auth strategy)
- Any non-obvious conventions or constraints
